class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        a=''
        for i in digits:
            a+=str(i)
        b=int(a)
        b+=1
        c=str(b)
        d=list(c)
        l=[]
        for k in d:
            l.append(int(k))
        return l
