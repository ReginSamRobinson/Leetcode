class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        s=str(n)
        l=list(s)
        p=1
        s=0
        for i in l:
            p*=int(i)
            s+=int(i)
        return p-s
