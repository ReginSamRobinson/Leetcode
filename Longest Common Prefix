class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        n=""
        s=sorted(strs)
        for i in range(len(s[0])):
            if s[0][i]==s[-1][i]:
                n+=s[0][i]
            else: break
        return n

